# Local verification — 4 September 2026

Implemented the personal monitor scope. No VM deployment, account system, payment flow or public access was created.

## Verified

- Production Next.js build and TypeScript checks pass.
- Twelve automated tests pass: two-check failure/recovery behavior, stale-gap reset, public-address filtering, local HTTP redirect/status/timeout fixture, actual PostgreSQL incident and outbox deduplication, invalidated result rejection, scheduler timestamp precision, Discord payload and rate-limit behavior, local SMTP acceptance, and host/origin guards.
- Database test uses a fresh `uptime_test_*` database and removes only that disposable database. Live application data remains in `uptime`.
- All four configured starting targets returned HTTP 200 from the local computer. Their names are emarket-svc.philgeps.gov.ph, emarket.philgeps.gov.ph, philgeps.gov.ph, and training.philgeps.gov.ph.
- One explicitly labeled Discord notification test was sent through the application outbox and persistent worker. The provider returned a receipt; delivery history reports sent, one attempt, no error. No simulated outage was sent to Discord.
- SMTP fixture test delivered an actual email to an isolated local receiver and verified the subject, content and recipient. After the owner supplied mail credentials, Gmail SMTP with required STARTTLS on port 587 accepted one labeled live test from admin@bayanko.ph to florenmoya@gmail.com. The normal application outbox and persistent worker recorded delivery #2 as sent on the first attempt at 2026-09-04 15:36:52 UTC, with no error. Inbox placement and reading were not verified. Credentials are stored only in ignored .env.local, using the application's existing SMTP_* and MAIL_* keys.
- Browser verified six monitor rows, two setup rows, name search, expanded editor, private-target rejection, save persistence, incident empty state and Discord delivery history.
- Browser measured no horizontal overflow and no clipped monitor grids at 1440, 650 and 390 pixels. Expanded editor and settings also fit at 390 pixels.
- An isolated browser test stopped refresh responses and advanced the browser clock by three minutes. All four previously healthy monitors changed to no-recent-check, the checker changed to offline, and the refresh-failure warning appeared. Live monitoring was not stopped for this test.
- Missing and foreign Origin requests return 403. A second real worker exits when the existing worker holds the advisory lock. `/api/health` reports a connected database and running worker.
- The finish reviewer scored its three findings resolved: intermediate-width clipping, accessible measurement labels/history counts, and dynamic setup instructions. This was a bounded source/screenshot review, not a live screen-reader test.
- The webhook is stored in ignored `.env.local`; it is not returned by the dashboard API or intentionally written into source/docs. Automatic incident alerts remain paused for local setup.

## Remaining input

The source status page hides all original monitor targets. The four domain-based monitors use tested HTTPS roots, which may differ from the original health-check paths. FACT PROD and FACT UAT remain unconfigured until their exact URLs are supplied. Email and Discord are configured and live test messages were accepted by both providers. Automatic incident alerts remain paused during local setup. The VM environment and deployment have not been inspected or changed.

Current local application: http://127.0.0.1:3100. Background process IDs and logs are recorded in ignored `.local/`. Screenshots are in ignored `.impeccable/review/`.
